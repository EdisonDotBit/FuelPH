@model IReadOnlyList<FuelPH.DTOs.FuelPriceDto>

<h1>FuelPH</h1>

@foreach (var price in Model)
{
    <div>
        <strong>@price.FuelType</strong>

        <span>
            ₱@price.PricePerLiter
        </span>
    </div>
}