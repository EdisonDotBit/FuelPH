@model IReadOnlyList<FuelPH.DTOs.StationDto>

<h1>FuelPH</h1>

<p>Stations found: @Model.Count</p>

@foreach (var station in Model)
{
    <div>
        <strong>@station.Name</strong>
        <span>
            (@station.Latitude, @station.Longitude)
        </span>
    </div>
}